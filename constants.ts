
import { PrintSize } from './types';

export const PRINT_SIZES: PrintSize[] = [
  { id: 'custom', name: 'Personalizado (Defina abaixo)', widthMm: 0, heightMm: 0 }
];

export const MM_TO_INCH = 0.0393701;
